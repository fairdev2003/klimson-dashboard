import { formatter } from '$lib/components/dashboard/dev/console/formatter';

export { formatter };

const red = formatter.red;
const orange = formatter.orange;
const bold = formatter.bold;
const italic = formatter.italic;
const tail = formatter.tail;

export { red, orange, bold, italic, tail };
