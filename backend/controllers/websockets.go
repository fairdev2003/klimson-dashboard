package controllers

// func (controller GlobalController) RegisterWebsocketEndpoints(groupPrefix string) {
// 	controller.websocket.Register("/stats/cpu", controller.websocket.InitializeHub("cpu"))
// }
